#include <iostream>

// comment
int main(){

    // Conditional Operation
    // (cond_expr) ? expre1 : expre2;
    /** 
      int num{};
      std::cout << "Enter an integer: ";
      std::cin >> num;
      
      std::cout << num << "is " << ((num % 2 == 0) ? "even" : "odd") << std::endl;
    */

   //-----------------------------------
   // Range-based for loop
   /*
   std::string name = "Anthony";
   
    for(char c : name)
        std::cout << c << std::endl;
    */
    
    return 0;
}